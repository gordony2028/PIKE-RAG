# Full-featured PIKE-RAG for Railway with lightweight alternatives
# Keeps all features while staying under 4GB limit

# Web framework
Flask>=2.3.0
Flask-CORS>=4.0.0
gunicorn>=21.2.0
Werkzeug>=2.3.0

# Essential PIKE-RAG core (lightweight parts)
openai>=1.0.0
python-dotenv
pickledb
dacite
jsonlines
markdown
tabulate
tqdm
requests
urllib3

# Document processing (lightweight)
PyPDF2
python-docx
openpyxl
beautifulsoup4

# Lightweight text processing
rank_bm25
rouge
word2number

# Vector database (lightweight config)
chromadb

# Essential langchain (core only, no heavy ML)
langchain-core
langchain-openai
langchain-community

# Lightweight NLP alternative to sentence-transformers
# Using OpenAI embeddings instead of local models