[build]
builder = "nixpacks"

[deploy]
startCommand = "python webapp/app_lightweight.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
# Environment variables will be set via Railway dashboard or CLI
PORT = 5001
PYTHONPATH = "."
PYTHONUNBUFFERED = "1"